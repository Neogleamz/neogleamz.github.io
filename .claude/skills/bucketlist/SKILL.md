---
model: sonnet
name: bucketlist
description: Pull and execute the next task from the SK8Lytz Bucket List ledger. Use when the user says "bucketlist", "next task", "work the ledger", or asks what to work on next.
---

# bucketlist (skill)

Execute the workflow defined in [.claude/commands/bucketlist.md](.claude/commands/bucketlist.md) — that command file is the single source of truth for the steps and output format. Read and follow it; do not duplicate the steps here.
