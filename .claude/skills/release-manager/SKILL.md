---
model: sonnet
name: release-manager
description: Cut a formal release with version bump and changelog. Use when the user says "release", "cut a release", or "bump the version".
---

# release-manager (skill)

Execute the workflow defined in [.claude/commands/release-manager.md](.claude/commands/release-manager.md) — that command file is the single source of truth for the steps and output format. Read and follow it; do not duplicate the steps here.
