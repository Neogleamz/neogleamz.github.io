# Orchestrator Plan

1. Map out the relevant files for Labelz Designer (HTML, CSS, JS).
2. Generate PROJECT.md detailing 4 key implementation milestones based on R1-R4.
3. Spawn a `teamwork_preview_explorer` to perform an initial deep dive on ViewBox calculation logic and PDF Preview UI code.
4. Spawn `teamwork_preview_worker` instances to implement fixes.
5. Spawn `teamwork_preview_reviewer` instances to verify.
6. Check for completion of all acceptance criteria.
