# Context

- R1: Dynamic ViewBox Scaling - Need to find where label scaling is calculated (JS) and CSS viewport settings.
- R2: Phantom QR Code Eradication - Requires `@page` and `@media print` updates in the main stylesheet for the label designer.
- R3: Native Label Rotation - Needs CSS transform and UI toggle in JS.
- R4: PDF Preview Integration - Find "PDF PREVIEW" button ID and bind `window.print()` to it in JS.
